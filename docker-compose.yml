services:
  backend:
    build:
      context: ./back-end
      dockerfile: Dockerfile
    environment:
      PORT: 3001
    ports:
      - "3001:3001"

  frontend:
    build:
      context: ./front-end
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "4173:80"
